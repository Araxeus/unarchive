name: Tests

on:
  - push
  - pull_request

jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
      - name: Yarn PnP Setup
        uses: Araxeus/setup-yarn-pnp-action@v1

      - name: Run test
        run: yarn test
